<Window x:Class="DollarConverterApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:DollarConverterApp"
        mc:Ignorable="d"
        Title="Glass Converter" Height="580" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanMinimize">

    <Window.Resources>
        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCFFFFFF"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="GlassInput" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" BorderBrush="#30FFFFFF" BorderThickness="1">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1A1F35" Offset="0.0"/>
                <GradientStop Color="#281C38" Offset="1.0"/>
            </LinearGradientBrush>
        </Border.Background>
        
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

            <Button Grid.Row="0" Command="{Binding CloseCommand}" 
                    HorizontalAlignment="Right" Content="✕" 
                    Foreground="White" Background="Transparent" BorderThickness="0"
                    FontSize="16" Cursor="Hand" Width="30" Height="30"/>

            <StackPanel Grid.Row="1" Margin="0,0,0,30">
                <TextBlock Text="Glass Dolar" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                
                <Viewbox Height="25" Margin="0,5,0,0">
                    <DatePicker SelectedDate="{Binding SelectedDate}" 
                                DisplayDateEnd="{Binding Mode=OneWay, Source={x:Static system:DateTime.Today}}"
                                Background="Transparent" BorderThickness="0" Foreground="White">
                        <DatePicker.Resources>
                            <Style TargetType="DatePickerTextBox">
                                <Setter Property="IsReadOnly" Value="True"/>
                                <Setter Property="Foreground" Value="#00C2FF"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Style>
                        </DatePicker.Resources>
                    </DatePicker>
                </Viewbox>
            </StackPanel>

            <Border Grid.Row="2" CornerRadius="15" Padding="20" Background="#15FFFFFF" BorderBrush="#30FFFFFF" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="🇺🇸 DÓLAR (USD)" Style="{StaticResource LabelText}"/>
                    <TextBox Text="{Binding UsdText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource GlassInput}"/>
                </StackPanel>
            </Border>

            <Grid Grid.Row="3" Margin="0,-15,0,-15" Panel.ZIndex="10">
                <Border Width="40" Height="40" CornerRadius="20" Background="#00C2FF">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="10" Color="#00C2FF" ShadowDepth="0" Opacity="0.6"/>
                    </Border.Effect>
                    <TextBlock Text="⇅" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" FontWeight="Bold" Margin="0,-2,0,0"/>
                </Border>
            </Grid>

            <Border Grid.Row="4" CornerRadius="15" Padding="20" Background="#15FFFFFF" BorderBrush="#30FFFFFF" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="🇧🇷 REAL (BRL)" Style="{StaticResource LabelText}"/>
                    <TextBox Text="{Binding BrlText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource GlassInput}"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="6" VerticalAlignment="Bottom">
                <Border Background="#10FFFFFF" CornerRadius="10" Padding="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="{Binding StatusMessage}" Foreground="#00C2FF" FontWeight="Bold"/>
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="1 USD = " Foreground="#80FFFFFF" FontSize="12"/>
                                <TextBlock Text="{Binding CurrentRate, StringFormat=C2, ConverterCulture=pt-BR}" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>

                        <Button Grid.Column="1" Command="{Binding RefreshCommand}" Content="⟳" 
                                Background="Transparent" Foreground="White" FontSize="24" BorderThickness="0"
                                VerticalAlignment="Center" Cursor="Hand"/>
                    </Grid>
                </Border>
                
                <TextBlock Text="{Binding LastUpdated, StringFormat='Atualizado às {0}'}" 
                           HorizontalAlignment="Center" Margin="0,10,0,0" Foreground="#40FFFFFF" FontSize="10"/>
            </StackPanel>

        </Grid>
    </Border>
</Window>